/**
 * Interfaz para definir cada columna de la tabla.
 * - key: propiedad del objeto que se mostrará en la celda.
 * - label: texto que se mostrará en el encabezado de la columna.
 */
export interface DataTableColumn {
  key: string;
  label: string;
}
